/**
	DECLARAÇÕES PARA ESTRUTURAS DE
		. CONTROLE *
		. CONDICIONAIS
		. LOOPS
		. EXCEÇÕES
**/
// estrutura de controle -> var
var a, b = 2, c; // 'a' e 'c' -> undefined

// estrutura de controle -> function
function dividir(x, y) {
	alert(x / y);
}
dividir(6, 2);

// estrutura de controle -> return
function multiplicar(x, y) {
	return x * y;
}
alert(multiplicar(3, 2));